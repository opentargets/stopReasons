---
title: "Study stop reasons"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output:
  github_document:
    toc: true
    toc_depth: 2 
---

# Clinical trial stop reason interpretation

```{r, Rsetup, include = FALSE}
library("tidyverse")
library("sparklyr")
library("sparklyr.nested")
library("lubridate")
library("ggdendro")
library("ggsci")
library("cowplot")
```

```{r, SparkConnection, include = FALSE}
options("sparklyr.verbose" = TRUE)
config <- spark_config()

# Allowing to GCP datasets access
config$spark.hadoop.fs.gs.requester.pays.mode <- "AUTO" # nolint
config$spark.hadoop.fs.gs.requester.pays.project.id <- "open-targets-eu-dev" # nolint

# spark connect
sc <- spark_connect(master = "yarn", config = config)
```

```{r, child=c('sentenceSimilarities.rmd')}
```

```{r, child=c('predictions.rmd')}
```

```{r, child=c('enrichments.rmd')}
```

## Specific questions about enrollment
## Safety-relevant targets
